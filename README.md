tool_behateditor provides a UI for editing the behat feature files.

It parses the behat_*.php files to look for available step definitions and does not use behat config.

See screenshots in pix/ folder
